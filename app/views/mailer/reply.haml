There's a new reply to your comment on "#{@comment.page.title}" at #{EMAIL_SITE_NAME}:
=""
-it = @comment.identity.icon_and_text
\[#{raw it[0]}] #{raw it[1]} wrote:
!=@comment.body.gsub(/^/,'> ')
=""
!=@link
