-div_for(page, :class => 'hentry') do
	%h2.entry-title= link_to_unless_current(page.title, page, :rel => 'bookmark')
	-begin
		.entry-content= Haml::Engine.new(page.body, :format => :html5).render
	-rescue
		.entry-content.hamlfail= page.body
