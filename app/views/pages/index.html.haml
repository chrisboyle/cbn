.hfeed
	-if params[:is_root]
		=render 'rootbanner'
	-else
		%h1 Listing pages

	.entries
		-if @pages.empty?
			.placeholder No pages or posts yet
		-else
			=render @pages

	-if permitted_to? :create, Page.new
		=link_to 'New page', new_page_path
