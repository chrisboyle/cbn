.hfeed
	-if params[:is_root]
		=render :partial => 'rootbanner'
	-else
		%h1 Listing pages

	.entries
		=render :partial => @pages

	-if permitted_to? :create, Page.new
		=link_to 'New page', new_page_path
